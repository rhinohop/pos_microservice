package com.masterclass.inventory.dto; import jakarta.validation.Valid; import jakarta.validation.constraints.*; import java.math.BigDecimal; import java.time.OffsetDateTime; import java.util.List; public class SaleDtos { public record SaleItemRequest(@NotNull Long productId,@Positive int quantity){} public record SaleRequest(@NotEmpty List<@Valid SaleItemRequest> items){} public record SaleDetailResponse(Long productId,String sku,String productName,int quantity,BigDecimal unitPrice,BigDecimal subtotal){} public record SaleResponse(Long id,OffsetDateTime saleDate,BigDecimal totalAmount,List<SaleDetailResponse> details){} }
